{
  "name": "university-event-manager-shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for University Event Management System",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "type-check": "tsc --noEmit",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "prettier": "^3.0.3",
    "rimraf": "^5.0.5"
  },
  "files": [
    "dist/**/*"
  ]
}
